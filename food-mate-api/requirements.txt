# LiteLLM 需安装 proxy 额外依赖（含 websockets/fastapi/uvicorn 等）才能启动网关
# 注意：litellm 的 proxy 模块与 fastapi 内部私有 API 强耦合，版本需锁定为已验证的组合，
# 否则 pip 会装到不兼容的最新 fastapi，导致 litellm proxy 启动时 ImportError 崩溃
litellm[proxy]==1.88.1
fastapi==0.136.3
uvicorn
sse-starlette
openai>=2.41.0
python-dotenv>=1.1.0
PyYAML>=6.0.2
requests>=2.32.0
beautifulsoup4>=4.13.0
httpx>=0.28.0
rich>=13.9.0
tiktoken>=0.9.0